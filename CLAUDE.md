# CLAUDE.md — ビアガーデン情報収集・Excel自動作成システム（シナリオ①）

このファイルは Claude Code がこのリポジトリで作業する際のプロジェクトガイドです。
実装を始める前に必ず本ドキュメントと `TASK.md` を読んでください。

## 1. プロジェクト概要

Linuxサーバー上で、利用者がブラウザから対象ビアガーデンを選択すると、
各ビアガーデンの公式サイト情報を自動収集・AI解析し、
Excelテンプレートへ転記した調査結果ファイルを出力するシステム。

## 2. システム構成

```
Linuxサーバー
├ beer-create.csv          # ビアガーデンマスタ(管理番号/名称/URL1/URL2)
├ beerinfo-create.html      # 利用者向け選択画面
├ n8n（Webhook）            # オーケストレーション層
├ Python
│ └ BeerInfo_Template.xlsm  # Excel出力テンプレート
└ 出力フォルダ               # 生成されたExcelファイルの格納先
```

### コンポーネントの役割分担

| コンポーネント | 役割 |
|---|---|
| beerinfo-create.html | ビアガーデン一覧表示・選択UI、開始ボタン |
| n8n (Webhook `/beergarden`) | CSV読込APIの呼び出し、一覧をHTMLへ返却 |
| n8n (Webhook `/execute`) | 選択されたビアガーデンの処理を起動 |
| Python（収集） | URL1/URL2へアクセスしHTML取得、不要情報除去、本文抽出 |
| Claude API | 抽出テキストからビアガーデン情報を要約・抽出、不足項目の追加収集 |
| OpenAI API | Claude抽出結果の整合性チェック・不足補完・表記ゆれ修正・フォーマット統一 |
| Python（Excel操作） | テンプレートコピー、シート作成、データ転記、保存 |

## 3. 処理フロー

1. **HTML起動**：利用者が `beerinfo-create.html` をブラウザで開く。
2. `beerinfo-create.html` → n8n Webhook `/beergarden` → CSV読込API を呼び出し、
   `beer-create.csv` からビアガーデン一覧を取得してHTMLへ返却。
3. **HTML画面**：一覧をチェックボックスで表示。利用者が対象を選択し「開始」を押下。
4. 「開始」押下 → n8n Webhook `/execute` を呼び出し、選択IDリストを渡す。
5. **Python（Web情報取得）**：選択されたビアガーデンごとに
   - URL1アクセス（URL2が存在する場合は追加アクセス）
   - HTML取得
   - ヘッダー／フッター／ナビゲーション／広告など不要情報の除去
   - 本文抽出 → AI解析用テキスト生成
6. **Claude API（情報抽出）**：本文テキストから下記項目を抽出・要約。
   不足項目があれば公式サイト内の関連ページ・公開情報を追加収集して補完。
7. **OpenAI API（検証・補正）**：Claudeの抽出結果に対し
   整合性チェック・不足項目補完・表記ゆれ修正・フォーマット統一を行い、最終形式へ統一。
8. **Python（Excel操作）**：`BeerInfo_Template.xlsm` をコピーし、
   ビアガーデンごとにシートを作成してデータを転記、
   `BeerInfo_YYYYMMDD_HHMM.xlsm` として出力フォルダへ保存。

## 4. データ定義

### 4.1 beer-create.csv（入力マスタ）

| カラム | 説明 |
|---|---|
| 管理番号 | ビアガーデンの一意ID |
| ビアガーデン名称 | 表示名 |
| URL1 | 情報収集元URL（必須） |
| URL2 | 情報収集元URL（任意・無い場合あり） |

### 4.2 抽出・転記項目（Claude API / OpenAI API 共通の最終フォーマット）

- 運営会社
- 開催期間
- 営業時間
- 定休日
- 雨天営業
- ビアタイプ
- 概要
- システム
- 料金(税込)
- フード情報
- ドリンク情報
- 紹介文1 / 紹介文2 / 紹介文3
- おすすめポイント1 / おすすめポイント2 / おすすめポイント3
- ビア紹介URL
- PRTIMES URL

### 4.3 Excelセルマッピング（`BeerInfo_Template.xlsm`）

| セル | 項目 |
|---|---|
| B3 | 管理番号 |
| B4 | 運営会社（指定URL内から取得） |
| B5 | ビアガーデン名称 |
| B7 | 紹介文1 |
| B10 | 紹介文2 |
| B13 | 紹介文3 |
| B18 | 開催期間 |
| B19 | 営業時間 |
| B20 | 定休日 |
| B21 | 雨天営業 |
| B22 | ビアタイプ |
| B23 | 概要 |
| B24 | システム |
| B25 | 料金(税込) |
| E3 | 公式URL |
| E4 | ビア紹介URL |
| E5 | PRTIMES URL |
| E8 | フード情報 |
| E12 | ドリンク情報 |
| E18〜E20 | おすすめポイント1〜3 |

> ⚠️ **要確認事項**：仕様上 E3 が「公式URL」と「ビア紹介URL」の両方に割り当てられており重複している。
> 実装前に発注者／利用者へ意図を確認すること（例：E3=公式URL、別セルにビア紹介URLを配置する等）。

### 4.4 出力ファイル

- ファイル名例：`BeerInfo_20260801_1215.xlsm`
- 構成：ビアガーデンごとのシート + 集計シート
- 出力先：`出力フォルダ`

## 5. 技術方針・注意事項

- **Python**：スクレイピングは `requests` / `BeautifulSoup` 等を想定。本文抽出時は
  ヘッダー・フッター・ナビゲーション・広告要素を除去するロジックを共通化すること。
- **運営会社取得**：B4は指定URL内のHTMLテキストから抽出する。取得不可の場合は空欄を許容する。
- **URL2未設定時の扱い**：URL2が存在しない場合は、URL1のみで処理を継続する。URL2があれば追加アクセスを行う。
- **Excel操作**：`.xlsm` を扱うため `openpyxl`（マクロ保持のため `keep_vba=True`）を想定。
  テンプレートは都度コピーしてから編集し、原本は変更しない。
- **AI連携の2段構成**：Claude APIで一次抽出→不足項目は追加収集→OpenAI APIで検証・補正、
  という順序を崩さないこと。各APIの呼び出し失敗時のリトライ／フォールバック方針は
  実装時に別途検討する（現状未定義）。
- **n8n**：Webhookエンドポイントは `/beergarden`（一覧取得用）と `/execute`（実行用）の2本。
- **文字コード／表記統一**：OpenAI APIによる補正ステップで表記ゆれを吸収する設計のため、
  Claude API側では無理に統一しようとせず、素直な抽出を優先する。

## 6. 未確定・要確認事項（実装前に解消すること）

1. E3セルの重複（公式URL / ビア紹介URL）の割り当て方法
2. 不足項目補完時の「追加収集」の範囲（クロール深さ・対象ページの絞り込み方法）
3. Claude API / OpenAI API の呼び出し失敗時のリトライ・タイムアウト方針
4. 出力フォルダの命名規則・保存先パスの確定
5. 集計シートの具体的な項目・レイアウト（現状仕様に記載なし）

作業を進める中でこれらが判明・決定した場合は、本ファイルと `TASK.md` を更新すること
