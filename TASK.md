# TASK.md — ビアガーデン情報収集・Excel自動作成システム 実装タスク

`CLAUDE.md` の設計内容に基づくタスク分解。上から順に着手することを推奨（依存関係あり）。
各タスクは着手前に「6. 未確定・要確認事項」の該当項目が解消されているか確認すること。

## Phase 0: 事前準備

- [ ] `CLAUDE.md` 6章の未確定事項を発注者／利用者に確認し、回答を反映
- [ ] `/tmp/beer-create.csv` から `beer-create.csv` のサンプルデータを取得し、管理番号/名称/URL1/URL2 を確認
- [ ] `BeerInfo_Template.xlsm` の実物を確認し、セルマッピング（B3〜E20）が実態と一致するか検証
- [ ] Claude API / OpenAI API のAPIキーを環境変数として準備（`.env` 等、リポジトリには含めない）
- [ ] n8nの稼働環境（Webhook URL・認証方式）を確認

## Phase 1: CSV読込API

- [ ] `beer-create.csv` を読み込みJSON配列で返却するAPI（n8n内Functionノード or 別Pythonエンドポイント）を実装
- [ ] 管理番号・ビアガーデン名称・URL1・URL2（null許容）を含む形式で返却
- [ ] 異常系：CSVが存在しない／空の場合のエラーハンドリング

## Phase 2: beerinfo-create.html（選択画面）

- [ ] ページロード時に n8n Webhook `/beergarden` を呼び出し一覧を取得
- [ ] チェックボックス付きビアガーデン一覧を描画
- [ ] 「開始」ボタン押下で選択された管理番号リストを `/execute` へPOST
- [ ] 処理中／完了のステータス表示（ローディング・完了メッセージ）
- [ ] 異常系：一覧取得失敗時のエラー表示

## Phase 3: n8n ワークフロー設計

- [ ] Webhook `/beergarden`：CSV読込API呼び出し → レスポンス整形 → HTMLへ返却
- [ ] Webhook `/execute`：選択IDリスト受領 → 対象ビアガーデン情報（URL1/URL2）をCSVから引き当て
- [ ] 対象ビアガーデンごとにPython処理（Phase 4〜6）を呼び出すノード構成を設計
- [ ] 全件処理完了後の完了通知（HTMLへのレスポンス or 別途通知）

## Phase 4: Python — Web情報取得

- [ ] URL1（存在すればURL2も）へHTTPリクエストしHTML取得
- [ ] ヘッダー／フッター／ナビゲーション／広告要素の除去ロジック実装
- [ ] 本文抽出 → プレーンテキスト化（AI解析用）
- [ ] 異常系：アクセス失敗（404/タイムアウト等）時のハンドリング・ログ出力

## Phase 5: Claude API — 情報抽出

- [ ] 抽出テキストを渡し、下記項目をJSON形式で抽出するプロンプトを設計
  - 開催期間／営業時間／定休日／雨天営業／ビアタイプ／概要／システム／料金(税込)／
    フード情報／ドリンク情報／紹介文1〜3／おすすめポイント1〜3／ビア紹介URL／PRTIMES URL
- [ ] 不足項目がある場合、公式サイト内の関連ページを追加収集するロジック
  （Phase 4の取得処理を再利用し、追加URLをクロール）
- [ ] APIレスポンスのパース・バリデーション（必須項目欠落時の扱いを定義）

## Phase 6: OpenAI API — 検証・補正

- [ ] Claude抽出結果を渡し、整合性チェック・不足補完・表記ゆれ修正・フォーマット統一を行うプロンプトを設計
- [ ] 最終フォーマット（Phase 5と同一項目セット）でJSON出力させる
- [ ] 補正前後の差分をログ化（デバッグ・品質確認用、任意）

## Phase 7: Python — Excel操作

- [ ] `BeerInfo_Template.xlsm` を出力フォルダへコピー
  （ファイル名：`BeerInfo_YYYYMMDD_HHMM.xlsm`）
- [ ] `openpyxl`（`keep_vba=True`）でテンプレートを開き、ビアガーデンごとにシートを複製
- [ ] セルマッピング表（CLAUDE.md 4.3）に従いデータを転記
- [ ] 集計シートへの反映（項目・レイアウトはPhase 0で確定した仕様に従う）
- [ ] ファイル保存、保存先パスをn8nへ返却

## Phase 8: 結合テスト

- [ ] CSV1件のみ選択 → Excel出力までのE2E動作確認
- [ ] 複数件選択時の並列／逐次処理の動作確認
- [ ] URL2が無いビアガーデンでの動作確認
- [ ] 情報取得に失敗するURLを含めた場合のエラーハンドリング確認
- [ ] Claude API / OpenAI API のレスポンス不正時（フォーマット崩れ等）の挙動確認

## Phase 9: 運用ドキュメント整備

- [ ] セットアップ手順（環境変数・依存パッケージ）をREADMEへ記載
- [ ] エラー時の切り分け手順（どのPhaseで失敗したか特定する方法）を整理

---

## 進め方の推奨事項

- Phase 1〜3（一覧取得〜選択UI〜n8n骨格）を先に通し、モックデータでE2Eの導線を確認してから
  Phase 4〜7（実データ収集・AI解析・Excel転記）に着手するとリスクが低い。
- Claude API と OpenAI API の呼び出しはコスト・レイテンシが発生するため、開発中はモックレスポンスで
  UI・n8nフローの検証を先行させることを推奨。
